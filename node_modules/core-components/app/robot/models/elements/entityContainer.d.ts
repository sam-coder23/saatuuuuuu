import { IElement } from '../../models/elements/base';
export declare class EntityContainer {
    private baseEntities;
    constructor(baseEntities: IElement[]);
    readonly Entities: IElement[];
}
