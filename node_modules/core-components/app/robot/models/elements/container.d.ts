import { Entity } from '../../models/base/entity';
import { EntityContainer } from '../elements/entityContainer';
import { FormGroup } from "@angular/forms";
export declare class Container {
    private entities;
    private entityContainer;
    containers: Container[];
    tabs: any;
    isTabbedContainer: boolean;
    model: Object;
    group: FormGroup;
    private groupElements;
    constructor(entities: Entity[]);
    /**
     * This method is responsible for initializing the form group
     * with all the validation.
     * @method initFormGroup
     */
    initFormGroup(): void;
    private buildFormGroup(container);
    readonly Entities: Entity[];
    EntityContainerInstance: EntityContainer;
}
