import { Type, ComponentRef } from '@angular/core';
import { IPopup, IBase, Container } from "../../package/bundle";
export declare class BaseEntity {
    component: Type<any>;
    componentData: IBase;
    container: Container;
    containers: Container[];
    componentReference: ComponentRef<any>;
    constructor(component: Type<any>, componentData: IBase);
}
export declare class Entity extends BaseEntity {
    callback: Function;
    constructor(component: Type<any>, componentData: IBase);
}
/**
 * avoid using callback as it will not bring any value
 */
export declare class PopupEntity extends Entity {
    closingCallback: Function;
    doneCallback: Function;
    constructor(component: Type<any>, componentData: IPopup);
}
