import { ViewContainerRef, EventEmitter, ComponentRef, ElementRef, OnDestroy, Type } from '@angular/core';
import { BuilderService } from "../../../services/builder.service";
export declare class PopupComponent implements OnDestroy {
    private el;
    private viewContainerRef;
    private builderService;
    private title;
    private popupBodyType;
    private popupFooterType;
    private popupBodyReference;
    private popupFooterReference;
    closing: EventEmitter<any>;
    done: EventEmitter<any>;
    cancelled: EventEmitter<any>;
    okText: string;
    cancelText: string;
    iconClose: string;
    reverseHeader: boolean;
    ngClass: string | string[] | Set<string> | {
        [klass: string]: any;
    };
    iconCloseVisible: boolean;
    footerVisible: boolean;
    constructor(el: ElementRef, viewContainerRef: ViewContainerRef, builderService: BuilderService);
    ngOnDestroy(): void;
    set Title(value: string);
    private accepted;
    private denied;
    private terminated;
    get PopupBodyType(): Type<any>;
    set PopupBodyType(type: Type<any>);
    get PopupFooterType(): Type<any>;
    set PopuFooterType(type: Type<any>);
    private appendBody;
    getBody(): ComponentRef<any>;
    private appendFooter;
    getFooter(): ComponentRef<any>;
    render(): void;
}
