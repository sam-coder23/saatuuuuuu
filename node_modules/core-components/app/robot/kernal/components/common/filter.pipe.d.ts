import { PipeTransform } from '@angular/core';
export declare class SearchPipe implements PipeTransform {
    transform(records: any, keys: Array<string>, searchKey: string, searchCriteria: any): any;
    private escapeRegExp;
}
