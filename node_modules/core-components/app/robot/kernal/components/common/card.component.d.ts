import { EventEmitter, OnInit } from "@angular/core";
import { IInput } from "../../../package/bundle";
export declare class CardComponent implements OnInit {
    onInit: EventEmitter<any>;
    ngOnInit(): void;
    private model;
    Model: any;
    private headings;
    Headings: Array<{
        key: string;
        type: string;
    }>;
    private actions;
    Actions: Array<IInput>;
    onAction: EventEmitter<any>;
    private onButtonAction($event, model);
}
