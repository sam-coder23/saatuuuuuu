import { EventEmitter, OnInit } from "@angular/core";
import { IInput } from "../../../package/bundle";
export declare class CardComponent implements OnInit {
    onInit: EventEmitter<any>;
    ngOnInit(): void;
    private model;
    set Model(value: any);
    get Model(): any;
    private headings;
    set Headings(value: Array<{
        key: string;
        type: string;
    }>);
    get Headings(): Array<{
        key: string;
        type: string;
    }>;
    private actions;
    set Actions(value: Array<IInput>);
    get Actions(): Array<IInput>;
    onAction: EventEmitter<any>;
    private onButtonAction;
}
