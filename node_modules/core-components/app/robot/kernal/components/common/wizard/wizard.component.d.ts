import { EventEmitter } from "@angular/core";
import { WizardStepComponent } from '../wizard/wizard-step.component';
import { InputSchema } from "../../../../package/bundle";
export declare class WizardComponent {
    private steps;
    heading: any;
    private previousButtonDataModel;
    private nextButtonDataModel;
    private finishButtonDataModel;
    PreviousButtonText: string;
    NextButtonText: string;
    FinishButtonText: string;
    previousButtonClicked: EventEmitter<{
        stepIndex: number;
        step: WizardStepComponent;
        steps: Array<WizardStepComponent>;
        nextButtonModel: InputSchema;
        previousButtonModel: InputSchema;
        finishButtonModel: InputSchema;
        source: any;
    }>;
    nextButtonClicked: EventEmitter<{
        stepIndex: number;
        step: WizardStepComponent;
        steps: Array<WizardStepComponent>;
        nextButtonModel: InputSchema;
        previousButtonModel: InputSchema;
        finishButtonModel: InputSchema;
        callback: Function;
        source: any;
    }>;
    finishButtonClicked: EventEmitter<{
        stepIndex: number;
        step: WizardStepComponent;
        steps: Array<WizardStepComponent>;
        nextButtonModel: InputSchema;
        previousButtonModel: InputSchema;
        finishButtonModel: InputSchema;
        source: any;
    }>;
    private stepIndex;
    private currentStep;
    ngAfterContentInit(): void;
    readonly Steps: WizardStepComponent[];
    private getButtonEventArgs(source);
    private previous(source);
    private next(source);
    private finish(source);
    private toggleButtonState();
    private updateStep(forward);
}
