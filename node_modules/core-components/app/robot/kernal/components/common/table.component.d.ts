import { EventEmitter } from "@angular/core";
export declare class TableComponent {
    selectedRows: any[];
    noDataMessage: string;
    headings: Array<any>;
    rows: Array<any>;
    rowSelected: EventEmitter<any>;
    searchKey: string;
    filterColumns: Array<string>;
    columnFilterDropdown: any;
    translateBoolean: boolean;
    yesValue: string;
    noValue: string;
    disableSelectionEvent: boolean;
    multiSelection: boolean;
    disableSelection: boolean;
    ngClass: string | string[] | Set<string> | {
        [klass: string]: any;
    };
    disableSearch: boolean;
    searchCriteria: {};
    /**
     * return all the heading
     */
    get Headings(): Array<any>;
    /**
     * return all the selected rows
     */
    get SelectedRows(): any[];
    UpdateHeadings(headings: Array<any>): void;
    /**
     * TBD - Seems to be unusable for now
     * User case 1 - reading nested properties
     * @param row the object which has to be looked for value
     * @param propertyName property which has to be looked
     */
    getValue(row: any, propertyName: string): any;
    /**
     * makes a row as selected and unselect if already selected
     * @param row row which has to be selcted
     */
    select(event: MouseEvent, row: any): void;
    onSearchKeyChange($event: string, headingName: string): void;
}
