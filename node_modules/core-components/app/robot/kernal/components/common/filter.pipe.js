"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var core_1 = require("@angular/core");
var SearchPipe = (function () {
    function SearchPipe() {
    }
    SearchPipe.prototype.transform = function (records, keys, searchKey, searchCriteria) {
        var regex = new RegExp(searchKey, "i");
        if (!keys || !keys.length || (Object.keys(searchCriteria).length === 0 && !searchKey)) {
            return records;
        }
        return records.filter(function (record) {
            var exists = false;
            if (searchCriteria && Object.keys(searchCriteria).length > 0) {
                for (var key in searchCriteria) {
                    exists = String(record[key]).match(new RegExp(searchCriteria[key], "i")) !== null;
                    if (!exists) {
                        break;
                    }
                }
            }
            else {
                keys.forEach(function (key) {
                    if (!exists) {
                        exists = String(record[key]).match(regex) !== null;
                    }
                });
            }
            return exists;
        });
    };
    return SearchPipe;
}());
SearchPipe = __decorate([
    core_1.Pipe({ name: 'search' })
], SearchPipe);
exports.SearchPipe = SearchPipe;
//# sourceMappingURL=filter.pipe.js.map