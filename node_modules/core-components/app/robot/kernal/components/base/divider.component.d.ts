import { IElement } from '../../../models/elements/base';
export declare class DividerComponent {
    model: IElement;
    get Model(): IElement;
    set Model(value: IElement);
    set value(value: any);
    constructor();
}
