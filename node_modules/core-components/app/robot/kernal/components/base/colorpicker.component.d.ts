import { OnInit, ElementRef, Renderer } from '@angular/core';
import { IColorPicker } from '../../../models/elements/base';
import { BaseComponent } from "../base/base.component";
export declare class ColorPickerComponent extends BaseComponent<IColorPicker> implements OnInit {
    private element;
    private renderer;
    elementRef: any;
    tabGroup: any;
    private selectedColorOrName;
    colors: string[];
    constructor(element: ElementRef, renderer: Renderer);
    ngOnInit(): void;
    private getSelectedColorOrName();
    /**
     * This method is show color palette on click on color box.
     */
    private showColorPaletteBox();
    /**
     *  This method close all previously opened color pickers
     */
    private closeOpenedColorPickers();
    /**
     * This method is called on initialization of the component.
     */
    private pickColor(color, name?);
    /**
     * This method is validate color input as per default values
     */
    validateColorData(color: string): string;
}
