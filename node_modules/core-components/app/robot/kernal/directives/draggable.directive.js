"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var core_1 = require("@angular/core");
var location_1 = require("../../models/common/location");
var DraggableDirective = (function () {
    function DraggableDirective(el, renderer) {
        this.el = el;
        this.renderer = renderer;
        this.allowDrag = true;
        this.moving = false;
        this.startLocation = null;
        this.previousZIndex = "";
        this.previousPosition = "";
        this.dragStart = new core_1.EventEmitter();
        this.dragEnd = new core_1.EventEmitter();
    }
    Object.defineProperty(DraggableDirective.prototype, "options", {
        set: function (setting) {
            if (setting !== undefined && setting !== null && setting !== "") {
                var element = void 0;
                this.allowDrag = !!setting;
                element = this.dragElement ? this.dragElement : this.el.nativeElement;
                if (this.allowDrag) {
                    this.renderer.setElementClass(element, "nd-draggable", true);
                }
                else {
                    this.renderer.setElementClass(element, "nd-draggable", false);
                }
            }
        },
        enumerable: true,
        configurable: true
    });
    DraggableDirective.prototype.ngOnInit = function () {
        if (this.allowDrag) {
            var element = this.dragElement ? this.dragElement : this.el.nativeElement;
            this.renderer.setElementClass(element, "nd-draggable", true);
        }
    };
    DraggableDirective.prototype.getPosition = function (x, y) {
        var left = parseInt(this.el.nativeElement.style.left);
        var top = parseInt(this.el.nativeElement.style.top);
        if (window) {
            left = parseInt(window.getComputedStyle(this.el.nativeElement, null).getPropertyValue("left"));
            top = parseInt(window.getComputedStyle(this.el.nativeElement, null).getPropertyValue("top"));
        }
        return new location_1.ElementLocation(x - left, y - top);
    };
    DraggableDirective.prototype.moveTo = function (x, y) {
        if (this.startLocation) {
            this.renderer.setElementStyle(this.el.nativeElement, "left", x - this.startLocation.x + "px");
            this.renderer.setElementStyle(this.el.nativeElement, "top", y - this.startLocation.y + "px");
        }
    };
    DraggableDirective.prototype.startMoving = function () {
        // get old z-index and position:
        this.previousZIndex = this.el.nativeElement.style.zIndex ? this.el.nativeElement.style.zIndex : "";
        this.previousPosition = this.el.nativeElement.style.position ? this.el.nativeElement.style.position : "";
        if (window) {
            this.previousZIndex = window.getComputedStyle(this.el.nativeElement, null).getPropertyValue("z-index");
            this.previousPosition = window.getComputedStyle(this.el.nativeElement, null).getPropertyValue("position");
        }
        // setup default position:
        var position = "relative";
        // check if old position is draggable:
        if (this.previousPosition && (["absolute", "fixed", "relative"].indexOf(this.previousPosition))) {
            position = this.previousPosition;
        }
        this.renderer.setElementStyle(this.el.nativeElement, "position", position);
        this.renderer.setElementStyle(this.el.nativeElement, "z-index", "99999");
        if (!this.moving) {
            this.dragStart.emit(this.el.nativeElement);
            this.moving = true;
        }
    };
    DraggableDirective.prototype.stopMoving = function () {
        if (this.previousZIndex) {
            this.renderer.setElementStyle(this.el.nativeElement, "z-index", this.previousZIndex);
        }
        else {
            this.el.nativeElement.style.removeProperty("z-index");
        }
        if (this.moving) {
            this.dragEnd.emit(this.el.nativeElement);
            this.moving = false;
        }
    };
    // Support Mouse Events:
    DraggableDirective.prototype.onMouseDown = function (event) {
        if (event.button == 2 || (this.dragElement !== undefined && event.target !== this.dragElement)) {
            return;
        }
        this.startLocation = this.getPosition(event.clientX, event.clientY);
        this.startMoving();
    };
    DraggableDirective.prototype.onMouseUp = function () {
        this.stopMoving();
    };
    DraggableDirective.prototype.onMouseLeave = function () {
        this.stopMoving();
    };
    DraggableDirective.prototype.onMouseMove = function (event) {
        if (this.moving && this.allowDrag) {
            this.moveTo(event.clientX, event.clientY);
        }
    };
    return DraggableDirective;
}());
__decorate([
    core_1.Output(),
    __metadata("design:type", Object)
], DraggableDirective.prototype, "dragStart", void 0);
__decorate([
    core_1.Output(),
    __metadata("design:type", Object)
], DraggableDirective.prototype, "dragEnd", void 0);
__decorate([
    core_1.Input(),
    __metadata("design:type", HTMLElement)
], DraggableDirective.prototype, "dragElement", void 0);
__decorate([
    core_1.Input(),
    __metadata("design:type", Object),
    __metadata("design:paramtypes", [Object])
], DraggableDirective.prototype, "options", null);
__decorate([
    core_1.HostListener("mousedown", ["$event"]),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [MouseEvent]),
    __metadata("design:returntype", void 0)
], DraggableDirective.prototype, "onMouseDown", null);
__decorate([
    core_1.HostListener("document:mouseup"),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], DraggableDirective.prototype, "onMouseUp", null);
__decorate([
    core_1.HostListener("document:mouseleave"),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], DraggableDirective.prototype, "onMouseLeave", null);
__decorate([
    core_1.HostListener("document:mousemove", ["$event"]),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [MouseEvent]),
    __metadata("design:returntype", void 0)
], DraggableDirective.prototype, "onMouseMove", null);
DraggableDirective = __decorate([
    core_1.Directive({
        selector: "[nd-draggable]"
    }),
    __metadata("design:paramtypes", [core_1.ElementRef, core_1.Renderer])
], DraggableDirective);
exports.DraggableDirective = DraggableDirective;
//# sourceMappingURL=draggable.directive.js.map