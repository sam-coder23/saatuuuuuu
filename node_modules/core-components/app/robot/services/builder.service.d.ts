import { Type, ComponentFactoryResolver, ViewContainerRef, ComponentRef, ComponentFactory } from '@angular/core';
export declare class BuilderService {
    private componentFactoryResolver;
    /**
     *
     * @param componentFactoryResolver
     */
    constructor(componentFactoryResolver: ComponentFactoryResolver);
    /**
     * Inserts a componet at where location
     * @param componentType the type of the component which has to be inserted
     * @param where where it has to be inserted
     */
    appendTo(componentType: Type<any>, where: ViewContainerRef): ComponentRef<any>;
    /**
     * Resolves the factory type for a component type
     * @param componentType
     */
    resolveFactory(componentType: Type<any>): ComponentFactory<any>;
    /**
     *
     * @param hostComponentType
     * @param where
     * @param contents
     */
    appendContents(hostComponentType: Type<any>, where: ViewContainerRef, contents: Array<ComponentRef<any>>): ComponentRef<any>;
}
