import { AfterViewInit, ViewContainerRef, OnDestroy, EventEmitter } from '@angular/core';
import { RoboHostDirective } from '../form/host.directive';
import { BuilderService } from '../../services/builder.service';
import { Container } from '../../models/elements/container';
import { APIConfiguration } from '../../models/configuration/config';
import { AppConfig } from "../../../config";
export declare class MainComponent implements AfterViewInit, OnDestroy {
    private builderService;
    private viewContainerRef;
    private appConfig;
    robotHost: RoboHostDirective;
    fid: String;
    apiConfiguration: APIConfiguration;
    container: Container;
    viewContainerReference: ViewContainerRef;
    autoload: boolean;
    constructor(builderService: BuilderService, viewContainerRef: ViewContainerRef, appConfig: AppConfig);
    done: EventEmitter<any>;
    ngAfterViewInit(): void;
    load(container: Container): void;
    /**
     * @param entity
     * some of these here should be const
     */
    private loadEntity;
    getModel(): void;
    ngOnDestroy(): void;
}
