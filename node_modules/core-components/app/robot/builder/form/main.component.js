"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var core_1 = require("@angular/core");
var host_directive_1 = require("../form/host.directive");
var button_component_1 = require("../../kernal/components/base/button.component");
var tabs_component_1 = require("../../kernal/components/base/tabs.component");
var select_component_1 = require("../../kernal/components/base/select.component");
var table_component_1 = require("../../kernal/components/common/table.component");
var popup_component_1 = require("../../kernal/components/common/popup.component");
var colorpicker_component_1 = require("../../kernal/components/base/colorpicker.component");
var builder_service_1 = require("../../services/builder.service");
var container_1 = require("../../models/elements/container");
var config_1 = require("../../models/configuration/config");
var MainComponent = (function () {
    function MainComponent(builderService, viewContainerRef) {
        this.builderService = builderService;
        this.viewContainerRef = viewContainerRef;
        this.done = new core_1.EventEmitter();
        this.autoload = false;
    }
    MainComponent.prototype.ngAfterViewInit = function () {
        //this.viewContainerReference = this.robotHost.viewContainerRef;
        //this.viewContainerReference.clear(); //This has to be done once
        if (this.autoload) {
            //should have  && !this.viewContainerReference but does not render with that
            // View container reference is not being passed from UI
            this.viewContainerReference = this.viewContainerRef;
            this.load(this.container);
        }
    };
    MainComponent.prototype.load = function (container) {
        var _this = this;
        var entities;
        this.container = container;
        entities = this.container.Entities;
        entities.forEach(function (entity) {
            _this.loadEntity(entity);
        });
        this.done.emit();
    };
    /**
     * @param entity
     * some of these here should be const
     */
    MainComponent.prototype.loadEntity = function (entity) {
        var componentReference;
        //this.viewContainerReference = this.robotHost.viewContainerRef;
        //viewContainerReference = this.robotHost.viewContainerRef;
        //viewContainerReference.clear(); // This has to be done once
        // in future - entities should be capabile of stating contents
        // once again - no rush
        if (entity.component === popup_component_1.PopupComponent) {
            var comp = void 0;
            componentReference = this.builderService.appendContents(entity.component, this.viewContainerReference, entity.componentData.contents);
        }
        else {
            componentReference = this.builderService.appendTo(entity.component, this.viewContainerReference);
        }
        componentReference.instance.formGroup = this.container.group;
        entity.componentReference = componentReference;
        // Type casting over here can cause some issues as we would need to know all types which can be added
        // We can for now safely remove that. its not mandatory
        //(<TextboxComponent>componentReference.instance).Model = entity.componentData;
        if (entity.component === table_component_1.TableComponent) {
            var tableComponent = componentReference.instance;
            var schema = entity.componentData;
            tableComponent.headings = schema.headings;
            tableComponent.rows = schema.rows;
            tableComponent.searchKey = schema.searchKey;
            tableComponent.disableSelection = schema.disableSelection;
            tableComponent.multiSelection = schema.multiSelection;
            tableComponent.selectedRows = schema.selectedRows;
            tableComponent.ngClass = schema.ngClass;
            tableComponent.rowSelected.subscribe(function (selectedModel) {
                if (entity.callback) {
                    entity.callback(selectedModel);
                }
            });
        }
        else {
            if (entity.component === popup_component_1.PopupComponent) {
                var popupComponent = componentReference.instance;
                var schema = entity.componentData;
                popupComponent.Title = schema.title;
                popupComponent.okText = schema.okText;
                popupComponent.cancelText = schema.cancelText;
                popupComponent.ngClass = schema.ngClass;
                popupComponent.footerVisible = schema.footerVisible;
                popupComponent.iconCloseVisible = schema.iconCloseVisible;
                popupComponent.done.subscribe(function (selectedModel) {
                    if (entity.doneCallback) {
                        entity.doneCallback(selectedModel);
                    }
                });
                popupComponent.closing.subscribe(function (selectedModel) {
                    if (entity.closingCallback) {
                        entity.closingCallback(selectedModel);
                    }
                });
            }
            else {
                if (entity.component === colorpicker_component_1.ColorPickerComponent) {
                    var colorPickerComponent = componentReference.instance;
                    colorPickerComponent.ngModelChange.subscribe(function (model) {
                        if (entity.callback) {
                            entity.callback(model);
                        }
                    });
                }
                else {
                    if (entity.component === select_component_1.SelectComponent) {
                        var selectComponent = componentReference.instance;
                        selectComponent.ngModelChange.subscribe(function (model) {
                            if (entity.callback) {
                                entity.callback(model);
                            }
                        });
                    }
                    else {
                        if (entity.component === button_component_1.ButtonComponent) {
                            // should turn into button entity - but no rush as it won't impact consumers
                            componentReference.instance.onClick.subscribe(function (model) {
                                if (entity.callback) {
                                    entity.callback(model);
                                }
                            });
                        }
                        else {
                            if (entity.component === tabs_component_1.TabsComponent) {
                                componentReference.instance.containers = entity.containers;
                            }
                            else {
                                if (componentReference.instance.ngModelChange) {
                                    componentReference.instance.ngModelChange.subscribe(function (model) {
                                        if (entity.callback) {
                                            entity.callback(model);
                                        }
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }
        //if(componentReference.instance && componentReference.instance.Model) {
        componentReference.instance.Model = entity.componentData;
        //}
        /*
        This event subscription can be removed now because of
        direct model binding achieved in component
        if(componentReference.instance.modelChanged) {
            componentReference.instance.modelChanged = (model => {
                //console.log(model.key,",",  model.value);
                if(componentReference.instance.constructor.name === "CheckboxComponent"){
                    if(!componentReference.instance.isNested){
                        let obj = {};
                        obj[model.value] = model.isChecked;
                        // This binding is no more needed, as i have clubbed it to component itself
                        //this.container.model[model.key] = obj;
                    }
                    else {
                        // This binding is no more needed, as i have clubbed it to component itself
                         //this.container.model[model.key] = model.nestedItems;
                    }
                }
                else {
                    //This binding is no more needed, as i have clubbed it to component itself
                    //this.container.model[model.key] = model.value;
                }
            });
        }
        */
    };
    MainComponent.prototype.getModel = function () {
        console.log("apiConfiguration", this.apiConfiguration);
        console.log("fid", this.fid);
        console.log("ContainerModel", this.container.model);
        // this.container.Entities[0].container.Entities.forEach(entity => {
        //     entity.componentData.toJSON()
        // });
    };
    MainComponent.prototype.ngOnDestroy = function () {
        console.log("removed", this);
    };
    return MainComponent;
}());
__decorate([
    core_1.ViewChild(host_directive_1.RoboHostDirective),
    __metadata("design:type", host_directive_1.RoboHostDirective)
], MainComponent.prototype, "robotHost", void 0);
__decorate([
    core_1.Input("fid"),
    __metadata("design:type", String)
], MainComponent.prototype, "fid", void 0);
__decorate([
    core_1.Input("api-config"),
    __metadata("design:type", config_1.APIConfiguration)
], MainComponent.prototype, "apiConfiguration", void 0);
__decorate([
    core_1.Input("container"),
    __metadata("design:type", container_1.Container)
], MainComponent.prototype, "container", void 0);
__decorate([
    core_1.Input("appendTo"),
    __metadata("design:type", core_1.ViewContainerRef)
], MainComponent.prototype, "viewContainerReference", void 0);
__decorate([
    core_1.Input("autoload"),
    __metadata("design:type", Boolean)
], MainComponent.prototype, "autoload", void 0);
__decorate([
    core_1.Output(),
    __metadata("design:type", core_1.EventEmitter)
], MainComponent.prototype, "done", void 0);
MainComponent = __decorate([
    core_1.Component({
        selector: 'robot',
        template: "\n     <!-- This does not need a template so finally we will remove this -->\n    "
    }),
    __metadata("design:paramtypes", [builder_service_1.BuilderService, core_1.ViewContainerRef])
], MainComponent);
exports.MainComponent = MainComponent;
//# sourceMappingURL=main.component.js.map